import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div className="m-5 flex-row justify-between rounded-lg bg-white">
      Home
      {/* <Link to="/">HOME</Link>
      <Link to="/Register">ABOUT</Link> */}
    </div>
  );
};

export default Header;
