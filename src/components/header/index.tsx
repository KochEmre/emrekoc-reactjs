import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div className="flex-row m-5 bg-white rounded-lg justify-between">
      Home
      {/* <Link to="/">HOME</Link>
      <Link to="/Register">ABOUT</Link> */}
    </div>
  );
};

export default Header;
